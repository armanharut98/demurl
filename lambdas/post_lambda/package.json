{
  "name": "post_lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:layer": "mkdir -p archives && zip -r post-layer.zip node_modules package.json package-lock.json && mv post-layer.zip ./archives",
    "build:lambda": "mkdir -p archives && zip post.zip ./src/post_lambda.js && mv post.zip ./archives"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.726.1",
    "adler-32": "^1.3.1",
    "crc": "^4.3.2"
  },
  "type": "module"
}
